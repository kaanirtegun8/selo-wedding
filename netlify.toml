# Netlify yapılandırması
[build]
  command = "npm run build"
  publish = "dist"

# SPA: tüm yollar index.html'e düşsün (Vue Router history mode için)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
